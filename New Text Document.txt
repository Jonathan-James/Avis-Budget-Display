
		var myImage = document.getElementById("myPhoto3");

		var imageArray=["img/bulb.png", "img/char.jpg", "img/squirt.png"];

		var imageIndex=0;

		function changeImage (){
			myPhoto3.setAttribute("src", imageArray [imageIndex]);
			imageIndex++;
			if(imageIndex>=imageArray.length){
				imageIndex=0;

			}
		}

		var intervalHandle = setInterval(changeImage, 500);

		myPhoto3.onclick=function(){

			clearInterval(intervalHandle);
		}